fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2
anthropic==0.40.0
pytest==8.3.4
# Direct dependency since Phase 7: fastapi.testclient is built on it.
# (anthropic pulls it in too, but the API tests use it directly.)
httpx==0.28.1
